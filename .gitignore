# Python
__pycache__/
*.pyc
.venv/

# Credentials — NEVER commit
.env
garmin_tokens/
*.session

# Build scripts (keep on disk, don't deploy)
build_dashboard.py
build_stats.py
make_pacing.py

# Scratch / experiment scripts (local only — keep out of the public repo)
_*.py

# Local-only data (don't expose raw data; only dashboard.html ships)
# Comment these out if you WANT to commit raw JSON too
# activities.json
# sleep.json
# wellness.json
# plan.json

# Session handoff / backup notes (local only)
SESSION_HANDOFF*.md

# OS
.DS_Store
Thumbs.db

# Excel temp files
~$*.xlsx
